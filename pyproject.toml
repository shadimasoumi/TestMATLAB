[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "PSProcessing"
authors = [{name = "Shadi", email = "Shadi.masoumi@cervo.ulaval.ca"}]
dynamic = ["version", "description"]
